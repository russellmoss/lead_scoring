# Broker Protocol Automation - Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# ===== GOOGLE CLOUD =====
# Project ID
GCP_PROJECT_ID=savvy-gtm-analytics

# Path to service account key file (for local development)
# In production (n8n), use service account credentials directly
GOOGLE_APPLICATION_CREDENTIALS=C:\Users\russe\Documents\Lead Scoring\Broker_protocol\service-account-key.json

# ===== ALERT SETTINGS =====
# Email alerts
ALERT_EMAIL_FROM=alerts@yourcompany.com
ALERT_EMAIL_TO=team@yourcompany.com,lead-scoring@yourcompany.com

# Slack webhook (optional)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ===== LOGGING =====
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ===== DEVELOPMENT FLAGS =====
# Enable debug mode (verbose output)
DEBUG=False

# Enable verbose output
VERBOSE=False

# ===== TESTING =====
# Enable test mode (process only first N firms)
TEST_MODE=False
TEST_LIMIT=100

# ===== BIGQUERY SETTINGS (optional overrides) =====
# These default to values in config.py, but can be overridden
# FINTRX_DATASET=FinTrx_data_CA
# OUTPUT_DATASET=SavvyGTMData

# ===== MATCHING SETTINGS (optional overrides) =====
# Confidence threshold for fuzzy matching (0.0 to 1.0)
# CONFIDENCE_THRESHOLD=0.60

# Manual review threshold
# MANUAL_REVIEW_THRESHOLD=0.85

# ===== n8n SETTINGS (for reference) =====
# These are set in n8n workflow, not used in Python scripts
# N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/broker-protocol
# N8N_API_KEY=your-n8n-api-key

# ===== NOTES =====
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values in .env
# 3. Add .env to .gitignore to avoid committing secrets
# 4. For production (n8n), set these as environment variables in n8n settings
